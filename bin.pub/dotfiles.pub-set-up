#!/usr/bin/env bash

cd

repo=dotfiles.pub

# create symlinks from user's bin to all scripts in bin.pub
ln -vsf ~/$repo/bin.pub/* ~/bin

# create symlinks from user's home dir to config files in repo
ln -vsf ~/$repo/.z* ~/
